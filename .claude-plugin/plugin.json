{
  "name": "call-summary",
  "version": "1.0.0",
  "description": "Саммари созвонов из Notion → страница на Vercel + пуш в Telegram. Запуск по расписанию через Routines.",
  "author": { "name": "skipaoff" },
  "homepage": "https://github.com/skipaoff/call-summary",
  "repository": "https://github.com/skipaoff/call-summary",
  "mcpServers": "./.mcp.json",
  "userConfig": {
    "source": {
      "type": "string",
      "title": "Источник записей: notion, zoom, telegram или all",
      "description": "Откуда брать созвоны. notion (Notion AI Meeting Notes), zoom (облачные записи, нужен Zoom Pro+), telegram (эконом: пересылаешь транскрипты боту) или all (все сразу).",
      "required": true
    },
    "notion_page_url": {
      "type": "string",
      "title": "Notion: ссылка на страницу записей встреч",
      "description": "Только для источника notion: URL страницы с записями созвонов.",
      "required": false
    },
    "telegram_bot_token": {
      "type": "string",
      "title": "Telegram bot token",
      "description": "Токен бота от @BotFather.",
      "sensitive": true,
      "required": true
    },
    "telegram_chat_id": {
      "type": "string",
      "title": "Telegram chat_id",
      "description": "Куда слать сводку (узнать: напиши боту, открой api.telegram.org/bot<token>/getUpdates).",
      "required": true
    },
    "vercel_token": {
      "type": "string",
      "title": "Vercel token",
      "description": "Vercel → Settings → Tokens → Create.",
      "sensitive": true,
      "required": true
    }
  }
}
